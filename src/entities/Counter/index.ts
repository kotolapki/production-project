import { CounterScheme } from './model/types/counterScheme';
import { counterReducer } from './model/slice/counterSlice';
import { Counter } from './ui/Counter';

export { type CounterScheme, counterReducer, Counter };
